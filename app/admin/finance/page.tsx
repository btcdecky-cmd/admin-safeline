"use client";

import { ArrowDownToLine, Database, LockKeyhole, ShieldCheck, WalletCards } from "lucide-react";
import { useState } from "react";

export default function FinancePage() {
  const [withdrawalOpen, setWithdrawalOpen] = useState(false);
  const [confirmed, setConfirmed] = useState(false);
  return <div className="flex flex-col gap-8 pb-10"><header className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><div className="mb-3 inline-flex items-center gap-2 rounded-full bg-lime-100 px-3 py-1 text-xs font-semibold text-lime-800"><LockKeyhole /> Admin only</div><h1 className="text-3xl font-semibold tracking-tight text-slate-900 md:text-4xl">Profit & earnings</h1><p className="mt-2 max-w-xl text-sm text-slate-500">Live accounting ledger for completed orders, provider spend, fees, refunds, and treasury liquidity.</p></div><button onClick={() => { setConfirmed(false); setWithdrawalOpen(true); }} className="inline-flex min-h-11 items-center justify-center gap-2 rounded-full bg-slate-900 px-5 text-sm font-semibold text-white"><ArrowDownToLine /> Withdraw to treasury</button></header><section className="grid grid-cols-2 gap-3 lg:grid-cols-5">{["Collected revenue", "Provider expenses", "Gross profit", "Refunds", "Net earnings"].map((label) => <div key={label} className="rounded-2xl border border-slate-200/70 bg-white p-4"><p className="text-xs font-medium text-slate-500">{label}</p><p className="mt-3 text-xl font-semibold text-slate-400">—</p><p className="mt-1 text-xs text-slate-500">No connected data</p></div>)}</section><section className="grid gap-6 lg:grid-cols-2"><div className="rounded-3xl border border-slate-200/70 bg-white p-6 shadow-sm"><h2 className="flex items-center gap-2 text-lg font-semibold"><WalletCards className="text-lime-600" /> Treasury controls</h2><p className="mt-2 text-sm text-slate-500">On-chain balances and accounting profit remain separate.</p><div className="mt-6 rounded-2xl border border-dashed border-slate-300 p-8 text-center"><Database className="mx-auto text-slate-300" /><p className="mt-3 text-sm text-slate-500">Connect a ledger and wallet provider to display balances.</p></div></div><div className="rounded-3xl bg-slate-900 p-6 text-white"><h2 className="text-lg font-semibold">Wallet configuration</h2><p className="mt-3 text-sm leading-6 text-slate-300">Merchant and treasury wallet addresses will appear here only after secure configuration. No automatic transfers are performed.</p><div className="mt-6 flex items-center gap-2 text-sm text-lime-400"><ShieldCheck /> Manual authorization required</div></div></section><section className="rounded-3xl border border-dashed border-slate-300 bg-white p-12 text-center"><Database className="mx-auto text-slate-300" /><h2 className="mt-4 text-lg font-semibold text-slate-900">No transactions connected</h2><p className="mt-2 text-sm text-slate-500">Connect the production payment and accounting ledger to display order, customer, token, signature, costs, fees, refunds, and net earnings.</p></section>{withdrawalOpen && <div className="fixed inset-0 z-50 flex items-end justify-center bg-slate-950/40 p-0 sm:items-center sm:p-4"><div role="dialog" aria-modal="true" className="w-full max-w-md rounded-t-3xl bg-white p-6 shadow-2xl sm:rounded-3xl"><h2 className="text-xl font-semibold text-slate-900">Authorize treasury transfer</h2><p className="mt-1 text-sm text-slate-500">No destination or amount is configured.</p><label className="mt-6 flex items-start gap-3 text-sm text-slate-600"><input type="checkbox" checked={confirmed} onChange={(event) => setConfirmed(event.target.checked)} className="mt-0.5 size-4 accent-lime-500" />I confirm that a configured business wallet and transfer policy will be used.</label><div className="mt-6 flex gap-3"><button onClick={() => setWithdrawalOpen(false)} className="min-h-11 flex-1 rounded-full border border-slate-200 text-sm font-semibold">Cancel</button><button disabled={!confirmed} onClick={() => setWithdrawalOpen(false)} className="min-h-11 flex-1 rounded-full bg-slate-900 text-sm font-semibold text-white disabled:opacity-40">Authorize</button></div></div></div>}</div>;
}
