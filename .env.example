# Production Safeline backend (customer API + admin ops)
SAFELINE_API_URL=http://127.0.0.1:3001
# Optional: long-lived admin JWT from POST /api/auth/login as an admin user
SAFELINE_ADMIN_TOKEN=

# Also expose to client components if needed:
# NEXT_PUBLIC_SAFELINE_API_URL=http://127.0.0.1:3001
